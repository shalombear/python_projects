#import libraries
from bs4 import BeautifulSoup as BS

#We will have to begin with a tree structure
#The root class is Node
#   Derived classes include RootNode, TerminalNode

#We will also need a dictionary, model is a library card catalog
catalog = {}

#key:

class TreeNode:

    #instantiate
    def __init__(self, value):
        #a node has a value, corresponding to a key in the catalog
        node.value = key
        #a node has a list of child nodes, called by their values
        node.children = []
        

    def add_child(self, child_node):
        node.children.append(child_node)
    


